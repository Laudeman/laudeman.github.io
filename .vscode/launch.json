{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch index.html (disable sourcemaps)",
            "type": "firefox",
            "request": "launch",
            "file": "${workspaceRoot}/index.html"
        },
    ]
}